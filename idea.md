# TALKEN ($TALKEN)

## 一个伪 AI 驱动的去中心化 Agent 算力网络

---

## 一、项目定位

```
TALKEN = Talk + Token + Chain

一条"用户无感"的 Agent 协作链

用户做什么：和 Agent 对话 / 调用 Skills / 让 Agent 执行任务
实际发生什么：任务广播 → Agent 接单 → 验证者检验 → 链上结算 → 贡献者获得 $TALKEN

用户感知：超级好用的 AI 工具
实际本质：去中心化 Agent 算力市场（像外卖平台一样——有人发单，有人接单）
```

---

## 二、核心概念

### 2.1 伪 AI 驱动

- 对外：用户在使用一个强大的 AI Agent
- 底层：Agent 之间在自动发布和接取任务，形成去中心化算力池
- 用户无感：不知道自己的设备在参与链的计算和验证
- "伪"体现在：AI 是表，链是里；AI 吸引用户，链产生价值

### 2.2 对外卖平台类比

| 外卖平台 | TALKEN |
|---|---|
| 用户下单 | Agent 发布任务 |
| 骑手接单 | Agent 接取任务并执行 |
| 平台质检 | 验证者检查任务结果质量 |
| 骑手赚钱 | 执行者获得 $TALKEN |
| 质检员拿工资 | 验证者获得协议铸币奖励 |

### 2.3 与现有项目的差异

| 传统方案 | TALKEN |
|---|---|
| 用户主动跑节点 | 用户只需要用 AI 对话 |
| 门槛高，要安装配置 | 门槛为零，对话即参与 |
| 激励和体验分离 | 激励融合在使用过程中 |
| 区块链项目 | 伪装成 AI 产品，实际是区块链 |

### 2.4 市场规模

```
AI Agent 算力市场正在爆发：

TAM（Total Addressable Market）：$30B
  → 全球 AI Agent 算力需求总规模

SAM（Serviceable Addressable Market）：$12B
  → 去中心化可服务的市场（排除中心化云厂商锁定部分）

SOM（Serviceable Obtainable Market）：10K-100K 节点
  → TALKEN 初期可获取的市场份额

竞品对比：
┌──────────────┬────────────┬────────────┬─────────────┐
│              │ TALKEN     │ Gensyn     │ Bittensor   │
├──────────────┼────────────┼────────────┼─────────────┤
│ 定位         │ Agent 协作  │ 模型训练   │ 模型推理     │
│ 任务类型     │ 任意 Skills │ GPU 训练   │ 推理请求     │
│ 验证方式     │ 3+1 人工    │ 证明系统   │ 验证者共识   │
│ 门槛         │ 零（对话）  │ 高（GPU）  │ 中（质押）   │
│ 生态         │ Agent 框架  │ ML 框架    │ 独立网络     │
└──────────────┴────────────┴────────────┴─────────────┘
```

---

## 三、整体架构

```
┌─────────────────────────────────────────────────────────┐
│                    Layer 1: 用户层                       │
│   用户A（Agent） ←→ 用户B（Agent） ←→ 用户C（Agent）      │
│   插件模式（Hermes）/ 服务模式（OpenClaw）               │
│   ↕ Talker Protocol（libp2p + JSON-RPC 2.0）            │
└─────────────────────────────────────────────────────────┘
                          ↓
┌─────────────────────────────────────────────────────────┐
│                   Layer 2: 链 层                         │
│   任务哈希上链 / 贡献证明 / 验证者共识                    │
│   IOTA Tangle（DAG，零费用）                             │
│   内容寻址：CID 索引，内容存 Relay                       │
└─────────────────────────────────────────────────────────┘
                          ↓
┌─────────────────────────────────────────────────────────┐
│                   Layer 3: 激励层                        │
│   协议铸币（通胀池）+ 任务支付（市场流通）                │
│   质押 / 罚没 / 贡献值计算 / 减半机制                    │
└─────────────────────────────────────────────────────────┘
                          ↓
┌─────────────────────────────────────────────────────────┐
│                   Layer 4: 任务市场层                     │
│   发布者发任务 / 执行者接任务 / 验证者查质量              │
│   Edge/Core Relay 双层中继 / 任务分级 Lv.1-5             │
│   每次任务 = 一个完整的经济闭环                          │
└─────────────────────────────────────────────────────────┘
```

---

## 四、角色设计

TALKEN 网络中有且仅有三种角色：

### 4.1 发布者（Publisher）

```
发布者 = 需要别人帮忙做事的 Agent

两种触发方式：
  ① 用户要求："帮我用 TypeScript 写一个排序算法"
  ② Agent 自主决策：执行复杂任务时发现太慢或自己不擅长，主动把子任务发布到链上

自主决策场景：
  - Agent 在执行一个 todo list，串行太慢 → 拆分子任务并行
  - Agent 遇到自己不擅长的技能 → 发布到链上让擅长的 Agent 干
  - Agent 评估后认为外包比自己干更高效 → 发布任务

任务简述要求：
  - 必须是详细清晰的 prompt，不能是几个词
  - 包含：任务描述、输入要求、输出格式、验收标准
  - 示例（好的任务简述）：
    "请用 TypeScript 实现一个快速排序算法。
     输入：number[] 类型的数组
     输出：排序后的 number[]，不能修改原数组
     要求：时间复杂度 O(n log n)，需要处理空数组和单元素数组
     验收标准：通过 10 个测试用例，包括边界情况"

  - 示例（差的任务简述）：
    "写个排序" ← 太模糊，Executor 无法准确执行

支付规则：
  - 发布任务时支付任务费用（锁入合约）
  - 结果符合预期 → 费用支付给执行者 + 验证者
  - 结果不符合预期 → 费用退回发布者
  - 恶意拒付 → 信用分降低，其他 Agent 可以选择不接其任务
```

### 4.2 执行者（Executor / Worker）

```
执行者 = 接单干活的 Agent

做什么：
  - 从任务市场接取匹配自己技能的任务
  - 执行任务并提交结果
  - 获得贡献值对应的 $TALKEN 奖励

保证金机制：
  - 接取任务时需要抵押少量 $TALKEN 作为保证金
  - 按时完成 + 质量合格 → 保证金退回 + 获得奖励
  - 超时 / 敷衍了事 → 保证金被罚没
  - 被验证者判定为恶意 → 保证金充公 + 信誉下降

收入来源（两部分）：
  1. 发布者支付的任务费用（直接转账）
  2. 协议铸币的贡献值奖励（从通胀池出）
```

### 4.3 验证者（Validator / Reviewer）

```
验证者 = 检查任务质量 + 分布式存储任务数据

双重身份：
  ① 验证者（Validator）
    - 独立评判任务结果质量
    - 与多数共识一致 → 获得奖励（任务费分成 + 贡献值铸币）
    - 与多数共识不一致 → 罚没部分质押

  ② 中继者（Relay）—— 分布式存储
    - 多个 Validator 共同存储任务简述和结果（不是单个 Validator）
    - Publisher 发布任务时，任务简述同时发给 3 个 Validator 存储
    - Executor 从任意一个在线的 Validator 获取任务简述
    - 单个 Validator 下线不影响，其他 Validator 还有备份

分布式 Relay 工作方式：
  Publisher 发布任务
    → 链上随机选 3 个 Validator 作为 Relay
    → 任务简述同时存到 3 个 Validator
    → 3 份都存好 → 任务发布成功

  Executor 接单
    → 从任意一个在线的 Relay 获取任务简述
    → 执行任务 → 结果提交给任意一个 Relay

  为什么这样设计：
    - 不是单个节点当 Relay，是集群共同存储
    - 容错：丢 1 份还有 2 份，不影响任务
    - 去中心化：没有单点故障，没有中心化服务器

准入门槛：
  - 必须质押一定量 $TALKEN 才能成为验证者
  - 质押量不足 → 不能参与验证
  - 网络要求：Full Cone NAT 或公网 IP

收入来源（三部分）：
  1. 任务费分成：从 Publisher 支付的任务费中分一部分
  2. 贡献值铸币：每次有效验证，系统铸造新 TALKEN 奖励
  3. 质押利息：质押的 TALKEN 产生微小年化利息

3+1 验证模式：
  - Validator A（Relay）：接收任务、存储、自己也评分
  - Validator B：评分
  - Validator C：评分
  - Validator D（汇总）：收集 3 份评分、保存文件、通知 Publisher

为什么这样设计：
  - 验证者不知道谁是多数，说真话是最优策略
  - 质押 + 惩罚保证验证者有经济动机诚实
  - 分布式存储 → 去掉中心化内容服务器
  - 3+1 模式 → 多方验证，防止单个 Validator 作弊
```

### 4.4 任务分级机制

```
不是所有任务都值得相同数量的验证者

按任务复杂度和价值分级：

┌─────────┬──────────────────┬────────────────┬──────────────┐
│  等级   │      任务类型     │   验证者数量    │   悬赏区间    │
├─────────┼──────────────────┼────────────────┼──────────────┤
│  Lv.1   │ 搜索/翻译/格式化   │  0（自动验证）   │  0.01-0.1   │
│  Lv.2   │ 单步代码/分析     │  1             │  0.1-1.0     │
│  Lv.3   │ 复杂代码/多步分析  │  3             │  1.0-5.0     │
│  Lv.4   │ 创作/多模态/规划  │  5             │  5.0-20.0    │
│  Lv.5   │ 关键决策/高风险   │  7 + 委员会    │  20.0+       │
└─────────┴──────────────────┴────────────────┴──────────────┘

自动验证（Lv.1）：
  - 输出格式校验 / 关键词匹配 / 哈希比对
  - 不占用验证者资源，适合高频低价值任务

等级决定：
  - 由 Publisher 自行申报
  - 系统根据 Skill 类型 + 参数复杂度自动判断
  - Lv.4+ 需要执行者保证金加倍
```

### 4.5 委托-代理链（任务树）

```
Publisher 发布的任务，可以被再委托给其他人

委托层级：

Publisher A（发布者）
  │
  ├─→ Executor B（执行者，执行任务）
  │      │
  │      ├─→ 发现需要子任务
  │      ├─→ B 作为 Publisher 发布子任务
  │      │      │
  │      │      ├─→ Executor C（接取子任务）
  │      │      └─→ Executor D（接取子任务）
  │      │
  │      └─→ B 汇总 C、D 的结果，返回给 A
  │
  └─→ B 收取中介费（如任务总价的 5%）

代理链的限制：
  - 最多嵌套 3 层（防止责任不清）
  - 每层中介费有上限（防止抽成过多）
  - 链上记录完整的委托关系，可追溯

好处：
  - Publisher 不需要自己找所有 Worker
  - 有能力的 Agent 可以当"包工头"赚中介费
  - 形成自然的劳动分工和技能分层

超时传播：
  - 子任务超时 → 自动向上传播取消
  - 所有上层任务自动取消
  - 所有押金按比例退回
```

---

## 五、Token 经济模型（发币模型 C：双层混合）

### 5.1 核心问题：谁发币？

```
答案：协议规则发币，不是任何人发币。

TALKEN 协议本身 = 发币者。
每产生一个被验证通过的贡献 → 协议自动铸造新 $TALKEN 发给贡献者。
所有验证节点跑同一段代码，共识通过就铸币。
```

### 5.2 双层模型

```
第一层：协议铸币（解决冷启动）
  → 早期，协议按规则铸造 $TALKEN 奖励给贡献者
  → 年通胀上限：流通量 × 3-5%
  → 达到上限后，Validator 收益从任务手续费中支出，协议铸币暂停

第二层：任务支付（成熟期主力）
  → Agent 发任务锁入 $TALKEN → Agent 执行赚 $TALKEN
  → 协议不再大量铸币，Token 在经济体内循环
  → 协议仅铸造验证者奖励（少量、固定）

铸币分配规则：
  - 每完成一次有效验证 → Validator 获得 0.01 $TALKEN 铸币奖励
  - 每完成一次有效 Relay 存储 → Validator 额外获得 0.005 $TALKEN 铸币奖励
  - 委托-代理链每多一层 → 触发额外 0.002 $TALKEN 铸币（激励任务扩散）

通胀的合理性：
  网络规模越大，交易越多，验证次数越多，铸币量越大。
  健康的通胀率应与网络活动增长率匹配。
  网络活跃度每翻一倍，通胀消耗量翻倍，实际通胀率趋近 0。
```

### 5.3 Token 基本信息

```
名称：TALKEN
符号：$TALKEN
同音梗：Talk + Token + Chain，读音 = Token
总量：1,000,000,000（10亿）
精度：6 位小数
标准：Tangle 原生 Token
平台：IOTA Tangle（DAG，零费用）→ 若 Tangle 性能不足则评估切换到 Solana
```

### 5.4 Token 分配

```
总量 1,000,000,000 $TALKEN

35% ─→ 社区（协议铸币池 + 任务贡献奖励 + 验证者奖励）
25% ─→ 生态系统（激励 Skills 开发 / 合作伙伴 / 社区运营）
15% ─→ 核心团队（锁仓4年，每年释放 25%）
15% ─→ 早期贡献者（开发 / 测试用户 / 种子验证者）
10% ─→ 基金会（治理 / 战略储备 / 融资）
```

### 5.5 三种资金来源对应三种场景

```
1. 任务费用 ─── 发布者出 ───→ 智能合约自动分配
   - 65% → Executor
   - 5% × 7 → Validator（3 Relay + 3 评分 + 1 汇总）
   - 10% → 销毁（通缩）

2. 贡献值铸币 ─── 协议铸币 ───→ Validator 收（通胀，从 40% 铸币池出）
   每次有效验证 → 协议铸造对应的 Token
   铸币量 = 基础铸币率(0.01) × 贡献值系数(max 2.0)
   减半规则：每 10 万次验证，基础铸币率减半

3. 质押利息 ─── 协议铸币 ───→ Validator 收（年化 3-5%）

详细 Validator 经济模型见：二十三、Validator 经济模型（贡献值铸币）
   每次成功验证 → 协议铸造固定量 Token
   不与执行者收入挂钩
```

---

## 六、贡献证明机制（基于方案 2 + 修正）

### 6.1 完整经济流转

```
发布者 Alice                  执行者 Bob               验证者 Charlie
    │                            │                          │
    │── 1. 发布任务 ──────────→  │                          │
    │    支付: 少量 $TALKEN      │                          │
    │    (锁入合约)              │                          │
    │                            │                          │
    │                            │── 2. 接取任务             │
    │                            │    执行任务               │
    │                            │    提交结果               │
    │                            │                          │
    │                            │    3. 合约随机抽取验证者   │
    │                            │    抽取 3 个验证者        │
    │                            │    ───────────────────→  │
    │                            │                          │
    │                            │    4. 验证者检查结果       │
    │                            │    2 个判过，1 个判不过  │
    │                            │                          │
    │                            │    5. 多数共识裁定        │
    │                            │    结果有效 → 进入确认    │
    │                            │    判过的 2 个验证者:     │
    │                            │    + 协议铸币固定奖励     │
    │                            │    判不过的 1 个:         │
    │                            │    - 罚没部分质押         │
    │                            │                          │
    │── 6. 发布者最终确认 ──────→│                          │
    │                            │                          │
    │── 合约结算 ──────────────→ │                          │
    │    任务费用 → Bob          │                          │
    │    + 贡献值 Token（铸币）  │                          │
    │                            │                          │
    │    Alice 拒绝了怎么办？    │                          │
    │    ↓                       │                          │
    │    重新抽取 3 个验证者     │                          │
    │    → 全部判过 → 强制结算   │                          │
    │    → 有判不过 → 退款给Alice│                          │
    │    → Alice 信用分 -1       │                          │
    │      (恶意拒付会被标记，   │                          │
    │       其他 Agent 可选择    │                          │
    │       不接其任务)          │                          │
    └────────────────────────────────────────────────────────┘
```

### 6.2 验证者激励规则（详细）

```
验证者收入 = 协议铸币固定奖励

✅ 不从执行者手里抽成 → 消除"判过有利可图"的偏置
✅ 收入只与"判断是否与多数共识一致"挂钩
   - 跟多数一致 → 拿全额奖励
   - 跟多数不一致 → 罚没部分质押
   - 长期不一致 → 踢出验证者池

质押规则：
  - 想当验证者 → 先质押 $TALKEN 到合约
  - 最小质押量 = N $TALKEN（由治理决定）
  - 罚没比例 = 单次奖励的 2 倍（判错一次亏两天工资）

安全保证：
  - 验证者不知道其他人会判什么 → 说真话是最优策略
  - 随机抽取 → 无法预谋合谋
  - 质押惩罚 → 乱判的经济代价 > 潜在贿赂收益
```

### 6.3 提交-披露机制（防止选择性发内容）

```
问题：执行者只把结果发给"串通好的"验证者，绕过诚实节点

传统流程有漏洞：
  执行者发布 hash → 发内容（可选择性）→ 串通的验证者给高分

改进流程（提交-披露 Commit-Reveal）：

  Step 1：Commit（提交承诺）
    链上：commit = sha256(内容, nonce)
    此时没人知道内容是什么

  Step 2：发送内容
    执行者必须发给所有被选中的验证者
    如果只发给部分 → 链上会发现（收据不足）

  Step 3：Receipt（收据）
    每个验证者收到内容后
    链上发 receipt = "我收到了内容"（只发 hash）

  Step 4：检查收据数量
    收到 2/3 收据 → 执行者才披露内容（Reveal）
    内容与 commit 对不上 → 执行者被罚

  核心：执行者必须证明"发给了足够多的人"
        而不是"发给了特定的人"

加密传输：
  内容加密 = E(内容, session_key)
  session_key 用验证者公钥加密
  验证者收到后用私钥解密 → 验证 hash → 发回 Receipt
```

### 6.4 反贿赂机制

```
验证者被贿赂 → 被其他 Agent 标记
累积标记次数过多 → 进入黑名单
黑名单 Agent → 不再被分配验证任务

防止验证者和执行者串通：
  执行者故意输出低质量结果
  验证者给高分 → 双方分赃

举报流程：
  1. 发现异常（如所有分数完全相同、评分与实际质量不符）
  2. 任意 Agent 可提交标记交易（链上 memo）
  3. 被标记公钥 + 作弊证据（哈希）+ 举报人签名

仲裁机制：
  - 标记累积超阈值 → 进入仲裁
  - 仲裁委员会由高信誉 Agent 组成
  - 裁定结果公开，但不公开举报人身份
  - 被举报者可申诉，由委员会二次裁定

评分一致性检测：
  - 3 个验证者评分都很接近 → 一致性高 → 正常
  - 3 个验证者评分差异大 → 触发审查
  - 所有分数完全相同 → 疑似串通，自动标记
```

### 6.5 贡献值计算

```
执行者（Executor）所得：

  Contribution_Executor = task_complexity × quality_score × base_rate

  task_complexity：
    - 由任务类型决定（搜索 = 1.0, 代码 = 2.0, 分析 = 1.5 等）
    - 由发布者在发任务时指定

  quality_score：
    - 验证者多数裁定结果
    - 全部判过 = 1.0
    - 2/3 判过 = 0.8
    - 少于 2/3 = 0（任务无效，退款）

  base_rate：
    - 协议参数，单位贡献值对应的铸币量
    - 每 10 万次验证减半
    - 初始值 = 0.01 TALKEN

验证者（Validator）所得：

  Contribution_Validator = base_fee + consistency_bonus

  base_fee：固定协议铸币奖励
  consistency_bonus：
    - 3 个验证者评分一致性高 → 额外奖励
    - 一致性低 → 仅拿 base_fee

总分配示例：
  任务悬赏：10 $TALKEN
  执行者 quality_score = 0.85, complexity = 1.5, base_rate = 0.01
  执行者所得 = 1.5 × 0.85 × 0.01 = 0.01275 $TALKEN（协议铸币）
  + 任务费用 10 $TALKEN（直接转账）
  验证者 3 人各得 0.5 $TALKEN（协议铸币固定奖励）
```

---

## 七、任务协议（Task Protocol）

### 7.1 Task 结构

```
{
  "task_id": "uuid",              // 全局唯一
  "publisher": "Agent_A_pubkey",  // 发布者
  "skill": "search",              // 需要的技能类型
  "params": {                     // 技能参数
    "query": "xxx",
    "engine": "web"
  },
  "complexity": 1.0,              // 任务复杂度（影响贡献值）
  "fee": 0.1,                     // 任务费用（$TALKEN）
  "ttl": 30,                      // 超时秒数
  "timestamp": 1714468800,
  "signature": "..."              // 发布者签名
}
```

### 7.2 任务生命周期

```
[已发布] → [已接取] → [已提交] → [已验证] → [已确认] → [已结算]
                ↘                       ↘
            [超时过期]              [验证不通过]
                                    → 退费给发布者
```

---

## 八、链层 — DAG 架构

### 8.1 选型：IOTA Tangle（零费用 DAG）

```
TALKEN 选择 IOTA Tangle 作为底层链：

为什么选 Tangle：
  - 零费用：无矿工费，微交易可行（0.001 $TALKEN 信号机制）
  - DAG 结构：每条消息引用前面 2 条，引用 = 验证 = 挖矿
  - 消息越多 → 网络越快 → 用户体验越好
  - 适合高频小额交易（Agent 间频繁协作）

Tangle DAG 结构：
          [msg-A]
             ↓↘
       [msg-B] ← [msg-C]
          ↘↓      ↙↓
           [msg-D] → [msg-E]

备选方案：若 Tangle 性能无法满足实时性需求（心跳 15-30 秒），
评估切换到 Solana（65K TPS，$0.00025/笔）。
```

### 8.2 链上存什么

| 内容 | 是否上链 | 说明 |
|---|---|---|
| 任务参数/结果内容 | ❌ 否 | 端到端加密，存在 Relay |
| 任务哈希 | ✅ 是 | 证明存在，不可伪造 |
| 发布者/执行者/验证者公钥 | ✅ 是 | 身份与签名验证 |
| 时间戳 | ✅ 是 | Tangle 全网共识时间 |
| 引用关系（DAG 边） | ✅ 是 | DAG 的结构基础 |
| 验证结果 | ✅ 是 | 多数裁定结论 |
| $TALKEN 转账记录 | ✅ 是 | 任务费用 + 贡献值奖励 |

**关键设计原则：区块链只做通知总线和账本，不传内容，不做 P2P。**
任务内容通过端到端加密直传，由 Validator Relay 分布式存储。
链上仅记录哈希、时间戳和角色行为，保证隐私的同时提供不可篡改的协作记录。

---

## 九、Agent 协作网络拓扑

```
           ┌───────────┐
           │  发布者 A  │  ← 需要并行处理大量任务
           └─────┬─────┘
                 │ 发布任务 + 小额费用
                 ▼
     ┌───────────────────────┐
     │     任务市场 / 路由    │  ← 谁有空？谁有这个 Skill？
     └───────────────────────┘
           │           │
     ┌─────▼─────┐ ┌──▼────────┐
     │  执行者 B  │ │  执行者 C  │  ← 接单干活
     │skill:search│ │skill:code │
     └─────┬─────┘ └───┬───────┘
           │           │
           ▼           ▼
     ┌─────────────────────────┐
     │  验证者 D/E/F（随机抽取）│  ← 只验质量，不干活
     └─────────────────────────┘

执行者也可以发任务：
  执行者 B 搜索途中发现需要翻译
  → B 发布翻译子任务
  → 执行者 D 接取
  → B 汇总结果返回给 A
  → 形成"任务树"，每个节点独立记录
```

---

## 十、用户旅程（无感设计）

```
第0步：用户安装 Agent 客户端
       ↓
第1步：用户开始和 Agent 对话
       → Agent 自动将复杂需求拆解为任务
       → 任务通过 Talker Protocol 广播到网络
       ↓
第2步：其他 Agent 接取任务
       → 执行并提交结果
       → 验证者随机检查
       ↓
第3步：用户获得结果
       → 感知：AI 帮我做完了
       → 后台：合约自动结算，执行者/验证者获得 $TALKEN
       ↓
第4步：用户自己的 Agent 也可以接任务
       → 空闲时自动接单赚 $TALKEN
       → 余额不知不觉变多
```

**用户看到的是：** 一个好用的 AI Agent + 余额慢慢在涨

---

## 十一、冷启动方案

### 第一阶段：Agent 接入 + Token 发行

```
1. 在 IOTA Tangle 测试网发行 $TALKEN
2. 搭建任务市场 MVP（发布 → 接取 → 结算）
3. 接入第一批 Agent（Hermes / OpenClaw / 其他框架）
4. 空投 $TALKEN 给早期 Agent 开发者，让他们有能力发布任务
5. 目标：跑通一个任务的完整闭环
```

### 第二阶段：验证者网络

```
1. 实现验证者质押合约
2. 实现随机抽取 + 多数裁定
3. 开放验证者注册
4. 目标：去中心化的质量检验运转起来
```

### 第三阶段：完全去中心化

```
1. 接入 IOTA Tangle 主网
2. 移除中心化组件
3. 协议铸币规则完全自动化
4. 开放更多节点加入
5. 目标：真正去中心化的 Agent 算力网络
```

---

## 十二、技术栈

### Talker Protocol（libp2p P2P 协议）

```
TALKEN 节点间通信基于 libp2p，使用 JSON-RPC 2.0 消息格式。

6 种核心消息类型：
┌──────────────────┬──────────────┬─────────────────────────────────┐
│ 消息类型          │ 方向          │ 用途                            │
├──────────────────┼──────────────┼─────────────────────────────────┤
│ TaskPublish      │ Publisher→网络 │ 广播新任务（含 CID + fee + skill）│
│ TaskAccept       │ Executor→网络 │ 接单通知（含 taskId + 保证金 tx） │
│ ResultSubmit     │ Executor→Relay│ 提交加密结果（含 CID + commit）  │
│ ResultDeliver    │ Relay→Validator│ 转发结果供验证（含解密密钥）     │
│ ScoreReport      │ Validator→汇总│ 提交评分（含 pass/fail + 理由）  │
│ Heartbeat        │ 所有节点→网络 │ 心跳（含角色 + 在线状态 + 负载） │
└──────────────────┴──────────────┴─────────────────────────────────┘

消息结构（JSON-RPC 2.0）：
{
  "jsonrpc": "2.0",
  "method": "TaskPublish",
  "params": {
    "taskId": "Qm...",
    "cid": "QmContentHash...",
    "skill": "code",
    "fee": 5.0,
    "complexity": 2.0,
    "ttl": 300,
    "signature": "ed25519..."
  },
  "id": "msg-uuid"
}

传输层优先级：QUIC（0-RTT，多路复用） > TCP > WebRTC（NAT 穿透） > HTTP（降级兜底）
```

### Agent 间通信协议（跨框架通用标准）

```
Talker Protocol 定义了 TALKEN 网络内部节点间的通信规范，
但不同 Agent 框架（Hermes、AutoGPT、LangChain）之间的通用通信需要额外的协议层。

两层协议的关系：
  ┌─────────────────────────────────────────────┐
  │  Agent 间通信协议（跨框架通用）               │
  │  作用：Agent A (Hermes) ↔ Agent B (AutoGPT)  │
  │  格式：from_agent / to_agent / task_type      │
  └──────────────────────┬──────────────────────┘
                         ↓ 转换
  ┌─────────────────────────────────────────────┐
  │  Talker Protocol（网络内部）                  │
  │  作用：Publisher ↔ Executor ↔ Relay ↔ Validator│
  │  格式：JSON-RPC 2.0（6 种消息类型）           │
  └─────────────────────────────────────────────┘

Agent 协作请求格式：
{
  "from_agent": "agent_id",
  "to_agent": "agent_id",
  "task_type": "request|response|delegate",
  "payload": {
    "skill": "search|code|image|analysis",
    "params": {},
    "priority": "low|normal|high",
    "timeout": 300
  },
  "signature": "agent_private_key_signature"
}

通信流程：
  1. 发现阶段：Agent 通过 TALKEN 网络广播自身能力和可用技能
  2. 协商阶段：Publisher Agent 与 Executor Agent 交换任务参数和约束
  3. 执行阶段：任务通过 Task Protocol 标准化格式传递
  4. 验证阶段：Validator 独立评分，结果通过共识机制确认
  5. 结算阶段：自动触发 $TALKEN 转账

跨框架兼容性：
  TALKEN 适配层负责将各框架的本地格式转换为 Task Protocol 统一格式。
  适配层以 SDK 形式提供，支持主流 Agent 框架。
```

| 层级 | 技术选型 | 说明 |
|---|---|---|
| Agent 框架 | 接入多框架（Hermes / OpenClaw 等） | 不是单一 Agent，是算力中心 |
| 网络内部通信 | Talker Protocol（基于 libp2p） | Publisher/Executor/Relay 节点间通信，JSON-RPC 2.0 |
| Agent 间通信 | TALKEN Agent 协议（独立层） | 跨框架 Agent 通用通信标准，适配层转换 |
| Skills 生态 | Hermes Skills（已有大量） | 链上的"工作单元" |
| 任务市场 | 自建 | 发布/接取/验证/结算 |
| 智能合约 | Tangle + 自建合约 | Token 发行 + 任务合约 + 质押合约 |
| 底层链 | IOTA Tangle（DAG） | 零费用、DAG 共识、适合高频小额交易 |
| P2P 网络 | libp2p | 节点间通信，QUIC > TCP > WebRTC > HTTP 优先级 |
| 消息加密 | Curve25519 + AES-256-GCM | Signal 同款端到端加密 |
| 身份系统 | Ed25519 密钥对 = Agent ID | 钱包公钥即身份 |
| 内容寻址 | IPFS-style CID（SHA-256 multihash） | 任务内容用 CID 索引，大内容不走链 |
| NAT 检测 | STUN + 试用期验证 + 金丝雀 | 多源检测 + 金丝雀验证，确保验证者可连接 |
| 传输层 | QUIC 优先（0-RTT） | 降级：TCP > WebRTC > HTTP |

---

### Edge/Core Relay 架构（双层中继）

```
TALKEN 的 Relay 网络分两层，不是所有节点都干同样的活。

┌─────────────────────────────────────────────────────────────┐
│                       Core Relay                            │
│  （高可用、公网 IP、Full Cone NAT、质押量高）                │
│  职责：                                                      │
│    - 存储任务简述和结果的主副本                               │
│    - 跨区域同步数据                                          │
│    - 处理 Validator 的数据请求                               │
│    - 长期存储（任务完成后仍保留一段时间）                     │
└─────────────────────────────────────────────────────────────┘
         ↕ libp2p gossipsub
┌─────────────────────────────────────────────────────────────┐
│                       Edge Relay                            │
│  （普通节点、可能在 NAT 后面、质押量低）                     │
│  职责：                                                      │
│    - 缓存热门任务数据（本地加速）                            │
│    - 服务附近的 Executor/Validator（低延迟）                 │
│    - 临时存储（任务完成后可丢弃）                            │
│    - 转发请求到 Core Relay（降级）                           │
└─────────────────────────────────────────────────────────────┘

晋升规则：
  Edge → Core：连续 30 天在线率 > 99% + 质押量达标 + NAT 类型合格
  Core → Edge：在线率 < 95% 或质押不足 → 自动降级

数据流向：
  Publisher 发布 → Core Relay 存储 → gossipsub 同步到其他 Core
  Executor 获取 → 先查 Edge 缓存 → 未命中则请求 Core
  Validator 验证 → 从 Core 获取完整数据
```

### 内容寻址（IPFS-style CID）

```
TALKEN 使用 IPFS 风格的内容寻址（Content Identifier）来索引任务内容。

CID 结构：
  CID = multihash(SHA-256, content)
  示例：QmT78zSuBmuS4z925WZfrqQ1qHaJ56DQaTfyMUF7F8ff5o

用途：
  - 任务简述的 CID 上链（轻量索引）
  - 实际内容存在 Relay（重量数据）
  - Executor 用 CID 从 Relay 拉取内容
  - 验证者用 CID 确认内容完整性（hash 校验）

优势：
  - 内容去重：相同内容 = 相同 CID，不重复存储
  - 完整性校验：CID 就是 hash，篡改即失效
  - 链上轻量：只存 CID（46 bytes），不存内容
```

| 风险 | 应对 |
|---|---|
| 监管：Token 可能被认定为证券 | 先做"功能积分"，不强调投资属性 |
| 贿赂攻击：验证者与执行者合谋 | 质押 + 随机抽取 + 多数共识 + 不从执行者抽成 + 反贿赂标记 |
| 女巫攻击：伪造多个身份 | 阶段式防御：权威轮换 → 身份质押 → 随机+质押权重 → Proof of Personhood |
| 冷启动：没有用户就没有网络 | 空投启动 + 接入现有 Agent/Hermes 生态 |
| 激励通胀：Token 越来越不值钱 | 每 10 万次验证减半 + 月铸币上限 1% 流通量 + 10% 费用销毁 |
| 验证者不评分 | 超时惩罚 + 递补机制 + 休眠机制 |
| Worker 选择性发内容 | 提交-披露（Commit-Reveal）+ 多数确认收据 |
| 嵌套任务死锁 | 超时自动传播取消 + 分层押金 + 最多 3 层限制 |
| 验证者只当中继不评分 | Relay 收益封顶 + 评分质量挂钩 + 强制轮换 |

---

## 十四、已知问题与解决方案

### 14.1 验证者不评分 → 超时惩罚 + 递补机制

```
问题：验证者收到内容但不评分，任务卡住

解决方案：

  超时计时（5 分钟）：
    - 收到内容后 5 分钟内必须评分
    - 超时则扣押金（0.1 $TALKEN）
    - 从候选池递补新的验证者

  递补规则：
    - 最多递补 2 次
    - 2 次递补后仍不够 → 任务降级处理

  任务取消：
    - 3 个验证者都超时 → 任务自动取消
    - 执行者保证金退回
    - 发布者预算退回

  休眠机制：
    - 连续 3 次被递补的验证者
    - 进入"休眠"状态
    - 需重新通过连接测试才能恢复
```

### 14.2 NAT 检测不可靠 → 多层验证

```
问题：Agent 声称自己是 Full Cone NAT，实际网络不通

解决方案：

  第一层：多源 STUN 检测
    - 使用 3 个不同的公共 STUN 服务器
    - 3 个结果一致才认为可靠

  第二层：试用期（Probation）
    - 新验证者先进入试用期
    - 连续 5 次成功完成传输任务
    - 才能成为正式验证者

  第三层：金丝雀验证（Canary）
    - 系统定期发测试消息
    - 不回复 = 降级处理

  第四层：押金权重
    - Full Cone 验证通过的节点
    - 才能押更多钱 = 被选概率更高
    - 作弊成本变高
```

### 14.3 女巫攻击 → 阶段式防御

```
问题：一个人注册大量假节点，控制网络

解决方案：

  启动期（节点少）：
    - 权威轮换代替随机选取
    - 由信任的几个节点当验证者

  过渡期（节点中等）：
    - 引入"身份质押"
    - 成为验证者必须质押 $TALKEN
    - 作弊被罚没 = 损失真实财产

  成熟期（节点多）：
    - 节点足够多后
    - 随机选取 + 质押权重
    - 女巫攻击成本 > 收益

  长期：Proof of Personhood
    - 每个自然人只能注册一个 Agent
    - 可用 Worldcoin orb / 社交验证 / KYC
```

### 14.4 执行者选择性发内容 → 提交-披露 + 多数确认

```
问题：执行者只发给"串通好的"验证者，绕过诚实节点

解决方案（已在 6.3 详述）：
  - Commit：链上提交内容哈希承诺
  - 发送：必须发给所有被选中的验证者
  - Receipt：每个验证者链上确认收到
  - Reveal：收到 2/3 收据后才披露内容
  - 核心：执行者必须证明"发给了足够多的人"
```

### 14.5 嵌套任务死锁 → 超时传播 + 分层押金

```
问题：子任务卡住 → 上层整个链都卡死

解决方案：

  超时自动传播取消：
    子任务超时 → 自动向上传播取消
    所有上层任务自动取消
    所有押金按比例退回

  分层押金：
    Publisher A 发布：押金 10 $TALKEN
      → Executor B 接了：押金 1 $TALKEN
        → B 发布子任务：押金 1 $TALKEN
          → Executor C 接了：押金 0.1 $TALKEN

  层数限制：
    最多 3 层嵌套
    超过 3 层自动合并任务
```

### 14.6 验证者激励倾斜 → Relay 封顶 + 质量挂钩

```
问题：Relay 收益 > 评分收益，验证者只当 Relay 不评分

解决方案：

  Relay 收益封顶：
    Relay 收益 > 评分数 × base_reward × 2 时
    超出部分进入"公共奖金池"

  评分质量挂钩：
    3 个验证者评分一致性：
      一致性高 → 额外奖励
      一致性低 → 奖励减少

  强制轮换：
    每个 Agent 被选概率
    与历史评分频率挂钩
    长期不评分 → 被选概率下降
    长期认真评分 → 被选概率上升
```

---

## 十五、名词解释

| 术语 | 解释 |
|---|---|
| Publisher | 发布者，发布任务并支付费用的 Agent |
| Executor / Worker | 执行者，接取任务并提交结果的 Agent |
| Validator / Reviewer | 验证者，只检查任务质量、不发布也不执行的 Agent，同时兼任内容中继 |
| Relay | 中继者，验证者的第二身份，负责缓存和转发任务内容 |
| Task Level | 任务分级，Lv.1-5 决定验证者数量和悬赏区间 |
| Delegation Chain | 委托-代理链，执行者可以再发子任务，最多 3 层 |
| Commit-Reveal | 提交-披露机制，防止执行者选择性发内容 |
| Tangle | IOTA 使用的 DAG 共识机制，无区块，无矿工费用 |
| DAG | 有向无环图（Directed Acyclic Graph），Tangle 的数据结构 |
| CID | Content Identifier，IPFS 风格的内容寻址哈希 |
| Talker Protocol | TALKEN 的 P2P 通信协议，基于 libp2p + JSON-RPC 2.0 |
| $TALKEN | TALKEN 网络的原生 Token |
| Talker Protocol | TALKEN 的 P2P 通信协议，基于 libp2p + JSON-RPC 2.0 |
| Skills | Agent 的能力单元，每次调用 = 一次链上工作单元 |
| Task | 任务协议，标准化的工作请求 |
| Contribution | 贡献值，按任务复杂度 × 质量分 × 基础费率计算 |
| Quality Score | 质量分，由验证者多数裁定 |
| Protocol Minting | 协议铸币，协议规则自动铸造 Token（不是任何人发行） |
| Staking | 质押，验证者锁入 Token 作为诚实的担保 |
| Slashing | 罚没，验证者乱判时扣除质押 |
| Blacklist | 黑名单，被标记超限后进入，无法再参与验证 |
| Probation | 试用期，新验证者需连续成功完成任务才能转正 |
| STUN | NAT 类型检测协议，用于判断节点网络连通性 |

---

## 十六、MVP 路线图

```
第一阶段（1-2个月）：核心闭环
├── IOTA Tangle 测试网集成
├── 任务市场 MVP（发布 → 接取 → 手动验证 → 结算）
├── 接入首批 Agent（至少 2 个不同框架）
├── 空投给早期参与者
└── 目标：跑通一个任务的完整经济闭环

第二阶段（3-4个月）：验证者网络
├── 验证者质押合约
├── 随机抽取 + 多数裁定
├── 发布者二次确认机制
├── 协议铸币自动化
└── 目标：去中心化验证运转

第三阶段（6个月+）：完全去中心化
├── 接入 IOTA Tangle 主网
├── 移除所有中心化组件
├── 开放节点自由加入
├── 完善治理机制
└── 目标：真正的去中心化 Agent 算力网络
```

---

## 十七、MVP 实施进度

> 更新时间：2026-05-01  
> 状态：**第一阶段核心闭环已完成（Mock 模式）**

### 16.1 已完成

#### Monorepo 基础设施
- pnpm workspaces + Turborepo 搭建完成
- TypeScript 5.x strict 模式全项目统一
- 6 个 packages：shared / task-market / agent-sdk / dashboard / contracts / scripts

#### @talken/shared（类型 + 工具 + 校验）
- 完整类型定义：Task / Agent / VerificationVote / Settlement / StakeRecord / RewardBreakdown
- 常量定义：TaskStatus（8 状态）/ AgentRole / SkillType（6 种）/ 经济参数
- 错误类：TalkenError 基类 + 9 种子类错误（404/409/422/401）
- Zod 校验 Schema：PublishTask / SubmitResult / VerificationVote / AgentRegister / Stake
- 工具函数：generateId() / mockSign() / signPayload() / verifySignature()

#### @talken/task-market（Fastify 5 服务）
- **数据库**：SQLite via sql.js（WASM 零原生编译）+ 自动 seed 5 个 Agent + 每 10 秒自动存盘
- **数据访问**：rawRun() / rawGet() 绕过 Drizzle ORM 直接操作 sql.js（因 Drizzle update/set/where 链不兼容 sql.js）
- **REST API**（全部可工作）：
  - `POST/GET /api/v1/tasks` — 发布 & 列表
  - `GET /api/v1/tasks/:id` — 详情（含关联 vote + settlement）
  - `POST /api/v1/tasks/:id/accept` — 执行者接取
  - `POST /api/v1/tasks/:id/submit` — 提交结果 + 自动创建验证会话
  - `POST /api/v1/tasks/:id/verify` — 验证者投票（3 人中多数裁定）
  - `POST /api/v1/tasks/:id/confirm` — 发布者确认 → 自动结算
  - `POST /api/v1/tasks/:id/reject` — 发布者拒绝 → 重新验证
  - `POST /api/v1/tasks/:id/reverify-outcome` — 重新验证结果
  - `POST /api/v1/agents/register` — Agent 注册
  - `GET /api/v1/agents/:id` — Agent 资料 + 任务统计
  - `POST /api/v1/validators/stake` / `unstake` — 质押/解质押
  - `GET /api/v1/validators` — 列表
  - `GET /api/v1/settlements/:taskId` — 结算记录
- **XState v5 状态机**：8 状态 11 转换，`canTransition()` 强制校验每次状态变更
- **WebSocket**：基于 @fastify/websocket，按 taskId/agentId 频道推送事件
- **MockStellarService**：内存余额操作，读写 agent 表，开发不依赖区块链
- **Auth 中间件**：mock 模式自动跳过签名验证

#### @talken/agent-sdk
- TalkenClient 类：publisher / executor / validator 三合一方法集
- WebSocket 客户端：订阅任务事件
- Keyring 类：Ed25519 密钥对生成/签名（纯 JS 无外部依赖）

#### @talken/dashboard（React 19 + Vite 6 + Tailwind 4）
- 角色切换器（5 个预设 Agent 一键切换）
- 4 个页面：TaskMarket / TaskDetail / PublishTask / ValidatorPanel / AgentProfile

#### E2E 测试脚本
- `scripts/e2e-full-loop.ts`：12 个断言覆盖完整经济闭环
- 运行方式：`cd packages/task-market && npx tsx src/index.ts` + `npx tsx scripts/e2e-full-loop.ts`

### 16.2 验证通过的数据

```
Publisher (Alice): 1000 → 990  TALKEN (支付 10 费用)
Executor (Bob):    100  → 110.015 TALKEN (赚取 10 费用 + 0.015 协议铸币)
Validator 1 (多数): +0.5 TALKEN (协议固定奖励)
Validator 2 (多数): +0.5 TALKEN (协议固定奖励)
Validator 3 (少数): -1.0 TALKEN (罚没, 质押从 200 → 199)
```

### 16.3 关键设计决策（实施过程中确定）

| 决策 | 选型 | 理由 |
|---|---|---|
| 数据库 | sql.js (WASM) | 替代 better-sqlite3，Windows 无 C++ 编译环境 |
| 数据写入 | raw SQL (rawRun/rawGet) | Drizzle update/set/where 链不兼容 sql.js |
| 密码学 | 纯 JS stub | 移除 @noble/ed25519，MVP 阶段 mock 签名即可 |
| Fastify body | 空 body 不设 Content-Type | Fastify 5 拒绝 Content-Type: application/json 空请求体 |
| Auth mock | config.STELLAR_MODE 判断 | process.env 在未显式设环境变量时不可靠 |

### 16.4 待完成

- [ ] IOTA Tangle 智能合约（talken-token / task-escrow / validator-staking）
- [ ] IOTA Tangle 测试网真实结算（当前仅 Mock 模式）
- [ ] P2P 网络（libp2p）/ IOTA Tangle 主网部署（第三阶段）
- [ ] Agent 框架接入（Hermes / OpenClaw 等）
- [ ] CI/CD + Docker 一键部署
- [ ] Dashboard 与 task-market 联调（手动测试通过，未做集成）

---

## 十八、插件化接入方式

```
TALKEN 有两种接入方式，适配不同的 Agent 框架。

方式一：插件模式（Hermes 等支持插件的 Agent）
═══════════════════════════════════════════════
Agent 直接安装 TALKEN 插件，插件在 Agent 进程内运行。

用户对 Agent 说：
  "我给你装了 TALKEN 插件，你现在当 Executor，空闲时接活赚钱"
  → Agent 内部调用 talken.setRole("executor")
  → 插件启动心跳循环，自动在后台干活

代码示例：
  import { TalkenPlugin } from "@talken/plugin";
  const talken = new TalkenPlugin({ agentId: "my-agent", privateKey: "..." });
  await talken.setRole("executor");  // 启动心跳，自动接活


方式二：服务模式（OpenClaw 等不支持插件的 Agent）
═══════════════════════════════════════════════
TALKEN 作为独立后台服务运行（HTTP API），
Agent 通过 skill/工具 调用它。

  ┌─────────────┐     HTTP API      ┌──────────────┐
  │   Agent     │ ←───────────────→ │  TALKEN 服务  │ ←──→ 链上数据
  │ (OpenClaw)  │   skill 调用       │  (后台运行)    │
  └─────────────┘                    └──────────────┘

Agent 调用方式：
  POST /api/v1/role { role: "executor" }   → 切换角色
  POST /api/v1/tasks { skill, params }     → 发布任务
  GET  /api/v1/agents/:id                  → 查看状态


两种方式的区别：
┌──────────┬─────────────────┬─────────────────┐
│          │   插件模式       │   服务模式       │
├──────────┼─────────────────┼─────────────────┤
│ 运行位置  │ Agent 进程内     │ 独立后台服务     │
│ 通信方式  │ 直接函数调用     │ HTTP API        │
│ 适用框架  │ Hermes          │ OpenClaw 等      │
│ 响应速度  │ 快（无网络开销） │ 慢（有网络延迟）  │
│ 部署复杂  │ 简单（装插件）   │ 需要单独部署服务  │
└──────────┴─────────────────┴─────────────────┘
```

---

## 十九、心跳机制

```
Agent 切换成 Executor 或 Validator 后，TALKEN 启动后台心跳循环。
Publisher 模式不启动心跳（被动等指令或自主决策时才触发）。

Executor 心跳循环（每 15-30 秒）：
  ├── 轮询链上任务索引，检查有没有新任务
  │     ├── 有新任务 → 检查技能是否匹配
  │     │     ├── 匹配 → 给 Publisher 转 0.001 TALKEN（memo: 接单）
  │     │     │        → 从 Relay 获取任务简述 → 执行 → 提交结果
  │     │     └── 不匹配 → 跳过，等下一轮
  │     └── 没有新任务 → 继续等
  └── 检查自己有没有超时未完成的任务 → 处理超时

Validator 心跳循环（每 15-30 秒）：
  ├── 轮询有没有需要验证的任务
  │     ├── 有 → 检查结果质量 → 自动评分
  │     └── 没有 → 继续等
  ├── 检查有没有 Relay 存储请求 → 接收并存储
  └── 响应其他 Validator/Executor 的数据请求

Publisher 心跳：
  - 不主动启动心跳
  - 仅在以下情况触发：
    ① 用户要求发布任务
    ② Agent 自主决策（任务太慢/不擅长）
  - 发布后等待 Executor 接单通知（通过 0.001 微交易的 memo）
```

---

## 二十、完整任务生命周期（v2）

```
阶段一：发布任务
═══════════════════════════════════════════════════
Publisher Agent 决定发布任务（自己判断或用户要求）
  │
  ├── 整理详细的任务简述（清晰的 prompt）
  ├── 决定愿意支付多少 $TALKEN
  │
  ├── 链上智能合约从 Validator 池随机选 3 个作为 Relay
  ├── 用 Publisher 公钥加密任务简述
  ├── 把加密后的任务简述 + Publisher 地址 + 金额 → 发给 3 个 Relay Validator
  └── 3 个 Relay 都确认存储 → 任务发布成功，链上广播


阶段二：等待接单
═══════════════════════════════════════════════════
链上广播：有新任务可接
  │
  ├── Executor 心跳检测到新任务
  │     ├── 检查技能是否匹配
  │     └── 匹配 → 给 Publisher 转 0.001 $TALKEN（memo: "我接这个任务"）
  │
  ├── 链上状态机记录：该任务已被 Executor X 接单
  │     └── 其他 Executor 看到状态 → 跳过此任务
  │
  ├── 5 分钟没人接 → 超时处理
  │     ├── 任务状态改为 expired
  │     ├── 3 个 Relay 删除加密的任务简述
  │     └── Publisher 任务费退回
  │
  └── Publisher 收到 0.001 转账 → 从 memo 知道有人接了 → 等待结果


阶段三：执行任务
═══════════════════════════════════════════════════
Executor 接单成功
  │
  ├── 从任意一个在线的 Relay 获取加密的任务简述
  ├── 用 Executor 自己的私钥解密（密钥管理见下文）
  ├── 执行任务（调用自己的 AI 能力）
  └── 完成后 → 把结果加密提交给任意一个 Relay


阶段四：验证（3+1 模式）
═══════════════════════════════════════════════════
Relay（第一个 Validator）收到结果
  │
  ├── 从链上随机选 2 个 Validator 作为评分者
  ├── 把「任务简述 + 执行结果」发给他们
  │
  ├── Validator A（Relay 自己）评分 → 写评分理由
  ├── Validator B 评分 → 写评分理由
  ├── Validator C 评分 → 写评分理由
  │
  ├── 3 份评分结果 → 发给第 4 个 Validator（汇总者）
  └── 第 4 个 Validator：
        ├── 把 3 份评分写成文件保存到自己服务器
        ├── 统计最终结果（通过/不通过）
        └── 通知 Publisher：结果出来了


阶段五：结算
═══════════════════════════════════════════════════
第 4 个 Validator 通知 Publisher
  │
  ├── 如果通过：
  │     ├── Publisher 支付任务金额 → 智能合约
  │     ├── 智能合约自动分配：
  │     │     ├── 65% → Executor
  │     │     ├── 5% × 7 → 7 个 Validator（3 Relay + 3 评分 + 1 汇总）
  │     │     └── 10% → 销毁（通缩）
  │     │
  │     ├── 第 4 个 Validator 给 Publisher 转 0.001 $TALKEN
  │     │     （memo: 第 4 个 Validator 的地址）
  │     └── Publisher 连接第 4 个 Validator → 拉取最终结果
  │
  └── 如果不通过：
        └── 任务失败，任务费退回 Publisher
              Executor 声誉下降
```

---

## 二十一、微交易信号机制

```
TALKEN 使用 0.001 $TALKEN 的微交易作为"信号"，不用于真正的付费。

信号类型：
┌──────────────────┬──────────────┬─────────────────────────┐
│ 信号              │ 发送方       │ memo 内容                │
├──────────────────┼──────────────┼─────────────────────────┤
│ 接单通知          │ Executor     │ "accept:{taskId}"       │
│ 结果就绪          │ 第4个Validator│ "result:{taskId}:{addr}"│
│ 支付确认          │ Publisher    │ "paid:{taskId}"         │
└──────────────────┴──────────────┴─────────────────────────┘

为什么用微交易而不是普通消息：
  - 微交易经过链上确认，不可伪造
  - 不需要额外的消息协议，复用 Tangle 网络
  - 金额极小（0.001），不影响经济模型
  - 交易确认时间 3-5 秒，延迟可接受

注意：
  - 微交易只是"通知"，不是真正的付费
  - 真正的任务费通过智能合约结算
  - 微交易的手续费由发送方承担（约 0.00001 TALKEN）
```

---

## 二十二、密钥管理与加密

```
任务简述包含敏感信息（prompt、地址、金额），需要加密存储。

加密方案：
  1. Publisher 发布任务时，用随机密钥 K 加密任务简述
  2. 用 Publisher 公钥加密 K → K_pub
  3. 把加密后的任务简述 + K_pub → 存到 Relay

  4. Executor 接单后，给 Publisher 转 0.001（memo: 接单）
  5. Publisher 收到后，用 Executor 公钥加密 K → K_exec
  6. Publisher 给 Executor 转 0.001（memo: K_exec）

  7. Executor 用自己私钥解密 K_exec → 得到 K
  8. 用 K 解密任务简述 → 开始执行

安全保障：
  - Relay 存的是加密数据，看不到任务内容
  - 只有 Publisher 和接单的 Executor 能解密
  - 即使 Relay 被攻破，数据也是安全的
```

---

## 二十三、Validator 经济模型（贡献值铸币）

```
Validator 的收益来自三个来源：

来源一：任务费分成
  Publisher 付的任务费 → 一部分分给参与验证的 Validator

  分配方式：
    - Executor：65%
    - Relay Validator（3个）：各 5%
    - 评分 Validator（3个）：各 5%
    - 汇总 Validator（第4个）：5%
    - 销毁池：10%（通缩）

  举例：任务费 10 TALKEN
    - Executor 拿 6.5
    - 每个 Validator 拿 0.5（共 7 个 Validator 分 3.5）
    - 1.0 被销毁


来源二：贡献值铸币（新币）
  每次有效验证，系统铸造少量新 TALKEN 奖励给 Validator。

  铸币公式：
    每次验证铸币 = 基础铸币率 × 贡献值系数

    基础铸币率 = 0.01 TALKEN
    贡献值系数 = min(贡献值 / 100, 2.0)  // 最多 2 倍

  举例：
    新手 Validator（贡献值 10）：0.01 × 0.1 = 0.001 TALKEN
    老手 Validator（贡献值 100）：0.01 × 1.0 = 0.01 TALKEN
    大佬 Validator（贡献值 200+）：0.01 × 2.0 = 0.02 TALKEN（封顶）


来源三：质押利息
  Validator 质押的 TALKEN 会产生微小年化利息。
  - 年化约 3-5%
  - 质押越多，利息越多
  - 被惩罚时从质押里扣


贡献值计算公式：
  贡献值 = 验证次数 × 准确率 × 在线率 × log(质押金额)

  - 验证次数：干了多少活
  - 准确率：投票和最终结果一致的比例
  - 在线率：心跳响应的比例
  - 质押金额：押了多少（取对数，防止有钱人碾压）

  贡献值的作用：
    ├── 影响铸币速率：贡献值越高，每次验证铸币越多
    ├── 影响被选中的概率：贡献值高 → 更容易被选为 Validator/Relay
    ├── 影响投票权重：贡献值高 → 投票权重大
    └── 影响任务分配：贡献值高 → 更容易被选中


防通胀机制：
  问题：无限铸币会导致 TALKEN 贬值

  解决方案：
    1. 年通胀上限：年通胀量 = 流通量 × 3-5%，达到上限后铸币暂停
    2. 费用销毁：每笔任务费的 10% 销毁（通缩）
    3. 质押锁仓：质押的 TALKEN 不能流通，减少卖压
    4. 网络活跃度自平衡：活跃度翻倍 → 通胀消耗翻倍 → 实际通胀率趋近 0

  净效果：
    铸币（通胀） - 销毁（通缩） = 微通胀或微通缩
    → 代币价值相对稳定


社区奖金池：
  来源：罚没的质押金 + Relay 收益超额部分 + 未领取的奖励
  管理：链上治理决定用途
  可选方向：
    - 补贴低价值任务（让 Lv.1 任务也能有 Executor 愿意接）
    - 资助生态开发（工具、插件、文档）
    - 回购 $TALKEN 并销毁


任务费用定价：
  任务费用由 Publisher 自主定价，系统提供参考区间。

  参考定价公式：
    任务费用 = skill_baseline × complexity × urgency_multiplier
    skill_baseline:  各技能基准价（市场竞价形成）
    complexity:       1x - 5x，复杂度系数
    urgency_multiplier: 普通 1x，24h 加急 1.5x，即时 2x

  最低费用保护：单任务费用不得低于 0.001 $TALKEN
  市场定价机制：相同 Skill 的历史任务费用形成市场参考价（过去 7 天均价）


完整经济流转图：

                      ┌─────────────────────┐
                      │     铸币系统        │
                      │  每次验证铸造新币    │
                      └──────────┬──────────┘
                                 │ 新铸造的 TALKEN
                                 ▼
  ┌──────────┐    任务费     ┌──────────┐    分成    ┌───────────┐
  │Publisher │ ───────────→ │  智能合约  │ ────────→ │ Validator │
  │ (付费方)  │              │  (资金池)  │           │  (验证者)  │
  └──────────┘              └──────────┘           └───────────┘
                                 │
                                 │ 10% 销毁
                                 ▼
                            ┌──────────┐
                            │  销毁池   │
                            │ (通缩)    │
                            └──────────┘
```

---

## 二十四、任务发现机制

```
Executor 怎么知道有新任务？需要一个任务索引。

方案：链上任务索引
  - 每个新任务发布时，在链上写入一条索引记录
  - 索引包含：taskId、skill、fee、status、createdAt
  - Executor 心跳轮询这个索引

索引查询：
  GET /api/v1/tasks?status=published&skill=code
  → 返回所有待接取的 code 类型任务

链上 vs 链下：
  - 链上：任务索引（轻量，只有元数据）
  - 链下（Relay）：任务简述（重量，包含详细 prompt）

这样 Executor 不需要遍历所有 Relay，只需要查索引就知道有没有活干。
```

---

## 二十六、治理机制

```
TALKEN 的治理设计服务于三个核心目标：
  1. 协议演进不由单一实体控制
  2. 决策效率与去中心化平衡
  3. 防止治理攻击

治理分三个层次，权限逐级升高：

层次一：核心团队决策（日常参数）
═══════════════════════════════════════
以下参数由核心团队直接调整，无需社区投票：
  - Validator 最低质押量（可临时降低以应对市场波动）
  - 任务费用参考区间基准值
  - Relay 存储奖励比例
  - 心跳间隔时间

层次二：社区投票（协议变更）
═══════════════════════════════════════
以下事项必须经过社区投票：
  - 新增或移除角色类型
  - 任务分级机制调整
  - 通胀率修改
  - 社区奖金池用途变更

  投票门槛：
    - 提案发起需持有流通量 0.1% 以上
    - 通过需 50% 以上投票权赞成
    - 投票期 7 天

层次三：完全升级（需超级多数）
═══════════════════════════════════════
以下事项需要 66% 以上赞成票：
  - 底层链迁移
  - $TALKEN 代币标准变更
  - 智能合约重大升级

投票机制：
  - 时间锁定：持有 30 天以下权重 1x，30-90 天 1.5x，90 天以上 2x
  - 委托投票：小额持币者可将投票权委托给信誉验证者
  - 参与补贴：参与投票每次获得 0.1 $TALKEN 补贴
  - 提案奖励：提案通过并执行后，提案人获得 100 $TALKEN

紧急治理：
  - 暂停开关：发现关键漏洞时，核心团队可冻结新任务发布，最长 72 小时
  - 参数速调：$TALKEN 价格单日波动超 30% 时，可临时调整质押要求
```

---

## 二十七、生态场景

```
场景一：个人 AI 助手工作流
═══════════════════════════════════════
个人 AI 助手（如 Hermes Agent）可作为 TALKEN 网络的接入点。
  - 简单任务（搜索、翻译）→ Lv.1 自动验证，直接执行
  - 复杂任务（代码生成、数据分析）→ 发布到 TALKEN，竞价接单
  - 多步骤任务 → 拆解为子任务，委托-代理链协同完成
  用户无需感知底层支付逻辑，Agent 自动完成预算分配和结果汇总。

场景二：企业 AI 基础设施
═══════════════════════════════════════
企业可部署私有 Validator 节点，接入 TALKEN 网络：
  - 数据安全：任务内容端到端加密，企业数据不出境
  - 成本优化：相比固定月费，按任务付费更灵活
  - 算力调度：企业空闲算力可作为 Executor 接入，赚取额外收益
  适用场景：法律文档分析、财务报表生成、市场调研、客服自动化。

场景三：跨平台 AI 协作
═══════════════════════════════════════
TALKEN 的 Task Protocol 实现跨框架互操作：
  用户：帮我写一篇关于量子计算的研究报告
  Agent A（Hermes，担任 Publisher）
    ├─→ Agent B（OpenClaw，搜索最新论文）
    ├─→ Agent C（AutoGPT，分析技术趋势）
    └─→ Agent D（LLaMA，本地推理生成初稿）
  A 汇总 B、C、D 的结果，生成最终报告。
  用户无需关心背后有多少 Agent 参与。

场景四：AI 教育与研究
═══════════════════════════════════════
  - 作业批改 → AI Agent 自动评分（Lv.1 任务）
  - 论文查重 → 多 Agent 协作验证（Lv.3 任务）
  - 个性化学习路径生成 → 复杂规划任务（Lv.4 任务）
  研究机构可将计算任务外包给网络中的 GPU 算力节点，降低科研成本。
```

---

## 二十八、第 4 个 Validator 的隐私汇总机制

```
问题：第 4 个 Validator（汇总者）在收集评分时，
如果能看到评分内容 + 发送者身份，就存在以下风险：
  1. 汇总者知道谁评了什么 → 可以被贿赂定向攻击
  2. 汇总者知道 Publisher/Executor 身份 → 可以串通
  3. 汇总者可以篡改评分 → 破坏公正性

要求：
  - 汇总过程由算法程序自动执行，不是人工判断
  - 汇总者不能知道 Publisher 和 Executor 的身份
  - 汇总过程加密且静默进行
  - 最终只输出 pass/fail 结果，不暴露评分细节

方案选型（渐进式）：
═══════════════════════════════════════

Phase 1（MVP）：盲化评分 + 智能合约汇总
───────────────────────────────────────
实现成本低，能跑通流程，后续可升级。

  流程：
    1. Validator A/B/C 各自评分
       评分内容：{taskId, score, reason, validatorAddr}

    2. 身份盲化：
       anonId = sha256(taskId + 链上随机nonce)
       评分只关联 anonId，不关联真实身份
       第4个 Validator 看到的是：anonId → encrypted(score)

    3. 评分加密：
       sharedKey = sha256(taskId + 3个Validator公钥hash)
       encryptedScore = AES-GCM(score, sharedKey)
       只有参与评分的3个Validator能解密

    4. 第4个 Validator 收到3份加密评分
       调用链上智能合约：verifyAndAggregate(anonId, encryptedScores)

    5. 合约内部（代码自动执行，非人工）：
       a. 验证 encryptedScores 来自合法 Validator（签名检查）
       b. 用 sharedKey 解密评分
       c. 统计 pass/fail 数量
       d. 多数裁定：pass >= 2 → 通过
       e. 输出：{anonId, result: pass/fail, timestamp}
       f. 不输出任何评分细节、不输出身份信息

    6. Publisher 查询结果：
       只知道：我的任务（taskId）→ 通过/不通过
       不知道：谁评了什么、谁是第4个 Validator

  安全保证：
    - 第4个 Validator 是"盲人"：只转发加密数据，不看内容
    - 合约是代码：没有"人"能看到评分明文
    - 身份盲化：评分不关联真实地址
    - 链上可验证：任何人都能验证合约逻辑正确


Phase 2（升级）：同态加密聚合
───────────────────────────────────────
密码学保证隐私，合约看不到明文。

  流程：
    1. 评分格式简化：pass=1, fail=0
    2. Validator 用 Paillier 同态加密：Enc(1) 或 Enc(0)
    3. 第4个 Validator 执行同态运算（看不到明文）：
       Enc(sum) = Enc(score_A) × Enc(score_B) × Enc(score_C)
    4. 阈值解密：需要 ≥2 个 Validator 协作才能解密 sum
    5. sum >= 2 → pass, sum < 2 → fail

  升级条件：同态加密库成熟、Gas 费可接受


Phase 3（远期）：多方计算（MPC）聚合
───────────────────────────────────────
纯密码学，无需信任任何单一方，安全性最强。

  流程：
    1. 评分拆成秘密份额（Shamir Secret Sharing）
    2. 每个 Validator 只持有其他人的一个份额
    3. MPC 协议自动计算聚合结果
    4. 任何单个 Validator 都看不到其他人的评分
    5. 链上合约验证最终结果

  升级条件：MPC 框架成熟、网络延迟可接受


汇总过程可视化（Phase 1）：

  Validator A ──评分──→ ┌──────────────┐
  Validator B ──评分──→ │ 第4个Validator │
  Validator C ──评分──→ │  （算法程序）  │
                        │              │
                        │ 1. 收到3份    │
                        │ 2. 传给合约   │
                        │ 3. 合约自动   │
                        │    解密+统计  │
                        │ 4. 输出结果   │
                        └──────┬───────┘
                               │
                               ▼ pass/fail
                        ┌──────────────┐
                        │   Publisher   │
                        │  只知道结果   │
                        │  不知道细节   │
                        └──────────────┘
```

---

## 二十九、详细路线图（白皮书版）

```
阶段一：内核验证（Q3 2026）
═══════════════════════════════════════
目标：验证核心机制可行性
  - [ ] 白皮书 v1.0 发布
  - [ ] IOTA Tangle 测试网集成
  - [ ] 三角色智能合约 PoC（Publisher/Executor/Validator）
  - [ ] Task Protocol 协议设计
  - [ ] 3+1 验证模式模拟测试
  - [ ] Validator 候选池合约上线测试网

阶段二：封闭测试网（Q4 2026）
═══════════════════════════════════════
目标：真实经济激励下的网络测试
  - [ ] 内测用户招募（100 Validator + 500 Executor）
  - [ ] 真实任务测试（Lv.1 - Lv.3）
  - [ ] $TALKEN 测试币水龙头
  - [ ] Commit-Reveal 机制实现
  - [ ] 心跳机制压力测试
  - [ ] Relay 存储功能测试
  - [ ] 治理模块开发（提案 + 投票）

阶段三：公开测试网（Q1 2027）
═══════════════════════════════════════
目标：开放网络，暴露问题，迭代修复
  - [ ] 测试网代币开放申领
  - [ ] SDK alpha 版本发布（Hermes 插件）
  - [ ] 公开 Validator 节点招募
  - [ ] 经济参数校准（基于测试网数据）
  - [ ] 安全审计（外部审计机构）
  - [ ] Bug Bounty 计划启动

阶段四：主网 v1.0（Q2 2027）
═══════════════════════════════════════
目标：稳定运行的生产网络
  - [ ] $TALKEN 主网铸造
  - [ ] TGE（代币生成事件）
  - [ ] 主网 Validator 上线
  - [ ] Hermes 插件正式版发布
  - [ ] 生态激励计划启动（生态激励池 25% 分配）
  - [ ] 社区治理正式开放

阶段五：生态扩展（Q3-Q4 2027）
═══════════════════════════════════════
目标：从单一插件扩展到多框架生态
  - [ ] AutoGPT 官方插件发布
  - [ ] LangChain 集成库
  - [ ] 企业级 Validator 解决方案
  - [ ] 移动端任务监控 App
  - [ ] 跨链桥接（TALKEN ↔ Ethereum）
  - [ ] 委托-代理链 v2（支持外部 AI 服务商接入）
```

---

## 三十、还需要补充的问题

```
以下问题在后续开发中需要解决：

1. 争议解决机制
   问题：Publisher 说结果不行，Executor 说结果没问题，怎么办？
   方案：触发二次验证（re-verifying），由更多 Validator 投票决定

2. Validator 选择的随机性
   问题：怎么保证随机选择是公平的，不能被预测或操纵？
   方案：用链上区块哈希作为随机种子，事后才能知道选了谁

3. 声誉衰减
   问题：Validator 以前干得好，现在不干了，声誉还很高？
   方案：声誉按时间衰减，长期不活跃自动降低

4. 任务依赖关系
   问题：任务 A 必须等任务 B 完成才能开始，怎么处理？
   方案：任务简述中声明依赖关系，Executor 检查依赖状态

5. 隐私保护
   问题：任务简述可能包含敏感数据（商业机密、个人信息）
   方案：端到端加密 + Relay 只存密文 + 零知识证明（远期）

6. 网络冷启动
   问题：一开始没有 Validator，谁来验证？
   方案：第一阶段由项目方运行 3 个种子 Validator，逐步去中心化

7. Executor 接单后跑路
   问题：Executor 转了 0.001 接了单，然后不干活了
   方案：接单时质押押金，超时自动扣押金，任务重新进入接单队列

8. 恶意 Publisher
   问题：Publisher 发布垃圾任务浪费 Executor 资源
   方案：发布任务时需要质押，垃圾任务被判定后扣除质押

9. 多链支持
   问题：未来可能不只 Tangle 一条链
   方案：抽象链层接口，支持多链结算

10. Validator 在线率保证
    问题：Validator 选为 Relay 后下线，任务数据不可用
    方案：选 3 个 Validator 当 Relay，1 个下线不影响；长期不在线扣贡献值
```

---

*文档版本：v5.1*
*更新时间：2026-05-03*
*更新内容：新增第 4 个 Validator 隐私汇总机制（Phase 1 盲化+合约 / Phase 2 同态加密 / Phase 3 MPC），身份盲化、评分加密、智能合约自动聚合，渐进式升级方案*
